import 'package:flutter/material.dart';
import 'package:flutter_app/StylesData.dart';

class RowData3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Row(children: [
      Expanded(child: TextStyle5("finally done with writing last sentence"))
    ]);
  }
}